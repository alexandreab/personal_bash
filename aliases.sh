#!/bin/bash

# Git Aliases
alias gb="git checkout"
alias gc="git commit"
alias gs="git status"
alias gd="git diff"
alias gw="git log --format='%an' --since='6 months ago' | sort | uniq -c | sort -rn"

alias climate="curl wttr.in/bsb"
